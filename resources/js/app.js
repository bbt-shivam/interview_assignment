import './bootstrap';

import $ from 'jquery';
window.$ = $;
window.jQuery = $;

import 'bootstrap';
import 'jquery-validation';

// $.ajaxSetup({
//     headers: {
//         'X-XSRF-TOKEN': $('meta[name="csrf-token"]').attr('content')
//     }
// });